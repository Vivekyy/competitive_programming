#include <bits/stdc++.h>
using namespace std;
#define read(type) readInt<type>()

#define pb push_back
#define mp make_pair
#define pii pair<int, int>
#define a first
#define b second
#define vi vector<int>
#define all(x) (x).begin(), (x).end()
#define umap unordered_map
#define uset unordered_set
#define imax INT_MAX
#define imin INT_MIN
#define len(x) (int((x).size()))
#define si set<int>;
#define mii map<int, int>;

#define printv(a) {for(auto u:a) cout<<u<<" "; cout<<"\n";}
int min(int a, int b) { if (a<b) return a; return b; }
int max(int a, int b) { if (a>b) return a; return b; }
std::string to_upper(std::string str) { std::transform(all(str), str.begin(), ::toupper); }
std::string to_lower(std::string str) { std::transform(all(str), str.begin(), ::tolower); }

#define ll long long
// #define int long long

void solve(){
    int a,b,n,mod;
    
}

int32_t main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    
    int tc; 
    cin >> tc;
    while(tc--) {
        solve();
    }
    return 0;
}